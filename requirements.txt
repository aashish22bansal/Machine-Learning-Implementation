# Runtime dependencies for running the notebooks.
#
# This file is ALSO the environment Binder builds for the website's live-compute
# button, so it must cover every third-party import in the published notebooks.
#
# NOTE: the package is `scikit-learn`. The separate `sklearn` package on PyPI is a
# deprecated shim that now hard-fails on install -- it used to be listed here and
# would break any fresh `pip install -r requirements.txt`.
numpy>=1.26
pandas>=2.0
matplotlib>=3.8
scikit-learn>=1.4
scipy>=1.11
statsmodels>=0.14
seaborn>=0.13
mplfinance>=0.12
yfinance>=0.2

# Required by the interactive slider notebooks in Data Analysis and Statistics.
# NOTE: ipywidgets output CANNOT render on a static site -- it needs a live kernel.
# Prefer plotly (below) for anything that must stay interactive on the website.
ipywidgets>=8.1

# Kernel-free interactivity. Plotly figures embed as self-contained JSON in the
# notebook output, so sliders/hover/zoom work on the published site with no server.
# Preinstalled in Google Colab, so notebooks authored there need no extra setup.
plotly>=5.20

# Binder needs a kernel in the image.
ipykernel>=6.29
